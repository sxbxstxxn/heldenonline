{% extends '1col.html.twig' %}

{% block title parent() ~ user.username %}

{% block maincontent %}

    <div class="card maincontent text-white bg-dark">
        <div class="card-header">
            {{ user.username }}
        </div>
        <div class="card-body">
            body
        </div>
        <div class="card-footer">
           footer
        </div>
    </div>

{% endblock %}

